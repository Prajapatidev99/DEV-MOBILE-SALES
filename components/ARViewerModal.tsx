// This component has been removed as the AR functionality is no longer needed.
import * as React from 'react';

const ARViewerModal: React.FC = () => {
    return null;
};

export default ARViewerModal;
